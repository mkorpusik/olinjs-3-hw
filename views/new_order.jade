extends layout

block content
  form(id="newform")
    h1= title
    div
    each ingredient, i in ingredients
      input(type="checkbox", name="ingredient", value=ingredient._id, class="ingredients") 
      | #{ingredient.name}
      br
    input(type="submit", value="Submit Order")